class Solution {
    public int largestElement(int[] nums) {
     int res=nums[0];
    for(int i=1;i<nums.length;i++){
                res=Math.max(nums[i],res);
        }
        return res;
    }
}
